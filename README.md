# React_Native-Quiz_App
A react-native quiz app written in react native using react navigation 5, with vanilla state management.

1. Clone the repo.
2. Make sure Expo is intalled.
3. Type in "Expo start" in the root directory.
